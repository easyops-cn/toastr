language: node_js
node_js:
  - "node"

install:
  - npm install -g gulp karma-cli
  - npm install

script:
  - npm test

cache:
  directories:
  - node_modules

branches:
  only:
    - develop
    - master 
